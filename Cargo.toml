[package]
name = "bevy-voxel-server"
version = "0.0.1"
edition = "2021"
authors = ["James Youngblood (soundeffects)"]
description = "Handles running the logic of a voxel game, for clients to connect to."
license = "MIT OR Apache"

[dependencies]
bevy = "0.7"
ndshape = "0.3.0"
block-mesh = "0.2.0"
bevy_flycam = "0.7"
